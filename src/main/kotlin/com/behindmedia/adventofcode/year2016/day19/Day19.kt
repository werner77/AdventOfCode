package com.behindmedia.adventofcode.year2016.day19

import com.behindmedia.adventofcode.common.*
import kotlin.math.*

private fun solve(n: Int) {
    // First remove all the even numbers

    // Then the last number gets from 1, unless the number was even, than 1 keeps
}

fun main() {
    val data = parseLines("/2016/day19.txt") { line ->
        line
    }
    println(data)
}